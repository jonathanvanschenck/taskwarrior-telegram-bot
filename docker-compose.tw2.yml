services:
  bot:
    build:
      context: .
      args:
        TW_VERSION: "2"
    volumes:
      - ./.tw2-test:/etc/task
    environment:
      - DB_DATA=/etc/task/botdb
      - CRON_DATA=/etc/task/cron
      - TW_TASKDATA=/etc/task/taskdata
      - TW_TASKRC=/etc/task/taskrc
    env_file:
      - .env
